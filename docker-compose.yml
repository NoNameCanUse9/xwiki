services:
  xwiki:
    build: .
    image: ghcr.io/nonamecanuse9/xwiki:latest
    ports:
      - "8080:8080"
    environment:
      XWIKI_HTTP_ADDR: ":8080"
    volumes:
      - xwiki-data:/data

volumes:
  xwiki-data:
